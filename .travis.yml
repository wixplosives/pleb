language: node_js

os:
  - linux

node_js:
  - 12

before_install:
  - curl -o- -L https://yarnpkg.com/install.sh | bash -s
  - export PATH=$HOME/.yarn/bin:$PATH

deploy:
  provider: npm
  email: alexshe@wix.com
  api_key: "$NPM_TOKEN"
  skip_cleanup: true
  on:
    branch: master
